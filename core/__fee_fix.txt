================================================================================
DEFINITIVE FEE AUDIT — 2026-07-25
================================================================================

KALSHI TRUE COST STRUCTURE:
  Commission:    0%     (Kalshi charges zero commission on weather markets)
  Mean spread:   3.1¢   (measured across all markets, live bid-ask midpoint)
  Slippage:      0.5¢   (expected fill degradation)
  -------------------------------
  Round-trip:    2.05¢  (= 3.1¢/2 + 0¢ + 0.5¢) = 0.0205 as fraction of $1
  
  Source of truth: core/market_cost_model.py → MARKET_COST_MODEL
    round_trip_fraction() = spread/2 + commission + slippage = 0.031/2 + 0.0 + 0.005 = 0.0205

FILE-BY-FILE AUDIT:

✅ core/market_cost_model.py — CORRECT. Source of truth. 0.031 spread, 0.0 comm, 0.005 slippage.

✅ core/paper_trading_engine.py — CORRECT. fee_rate=0.0205 (line 289). Matches MARKET_COST_MODEL.
❗ BUT: fee calculation on line 1738 uses position_size (dollar notional) instead of quantity
    (contract count). For $100 at 50¢ = 200 contracts. Charges $2.05 instead of $4.10 = 2x undercount.
    FIX: quantity = round(position_size / effective_price); fee_cost = abs(quantity * self.fee_rate)

✅ core/unified_backtest.py — CORRECT. Imports MARKET_COST_MODEL.round_trip_fraction() as FEE_RATE.

❗ core/position_sizing.py — MIXED. Line 53: PositionSizingConfig.fee_rate=0.0 (WRONG).
    Line 73: KellyPositionSizer.__init__(fee_rate=0.0205) (CORRECT).
    FIX: Change line 53 to import from MARKET_COST_MODEL.

❌ core/fee_aware_kelly_position_sizing.py — WRONG DEFAULT. Line 22: fee_rate=0.0.
    Comment says "Kalshi charges 0 commission (spread-only costs)" which conflates commission 
    with total trading cost. The sizer's calculate_fee_adjusted_edge() is broken at 0.0 — it 
    computes edge as if there's no cost at all. Then the variance estimate hardcodes 0.95 
    return for wins (line ~88: "net_return = 0.95") — this IS a 5% cost but using a magic 
    number that doesn't match MARKET_COST_MODEL.
    FIX: Import fee_rate from MARKET_COST_MODEL, fix net_return to use correct cost.

❌ core/trade_execution.py — NOTIONAL/CONTRACT BUG. Line 460: fee_cost = abs(position_size * self.fee_rate).
    position_size is $100, not 200 contracts. For $100 at 50¢: charges $2.05 instead of $4.10.
    FIX: Compute quantity first, then apply fee_rate to quantity.

✅ core/pnl_tracking.py — UNCERTAIN (depends on what t[0] is). Line 252: 
    total_fees = sum(abs(t[0]) * self.fee_rate for t in today_all_trades)
    t[0] appears to be trade[0] = position_size_usd from DB. If so: same notional bug.
    FIX: Change to quantity-based calculation matching MARKET_COST_MODEL.

🟡 core/fee_aware_filter.py — TECHNICALLY OK but CONFUSING. FEE_RATE=0.0 is correct for 
    commission (Kalshi = 0%). Spread cost is handled by separate 'spread' parameter = 0.02.
    The total cost approximation is FEE_RATE cost = 0 + spread = ~2¢, close to 2.05¢.
    RISK: Another developer could think "fee_rate=0.0" means no cost at all.
    FIX: Rename FEE_RATE to COMMISSION_RATE=0.0, keep spread=0.02. Better yet, import from 
    MARKET_COST_MODEL.

🟡 core/kelly_position_sizer.py — DEFAULT_COST_FRACTION = 0.031 — uses raw spread (3.1¢) instead 
    of round-trip fraction (2.05¢ = spread/2 + slippage). This is roughly 1.5x the correct value.
    FIX: Import ROUND_TRIP_FEE from MARKET_COST_MODEL instead of hardcoding 0.031.

🟡 core/instance_config*.py — fee_rate=0.0 in all 3 variants. These are test/example configs.
    FIX: Update to use correct 0.0205.

ACTION ITEMS:

1. Add ROUND_TRIP_FEE constant to market_cost_model.py:
   ROUND_TRIP_FEE = (MEASURED_MEAN_SPREAD / 2.0) + DEFAULT_COMMISSION + DEFAULT_SLIPPAGE

2. In fee_aware_kelly_position_sizing.py:
   - Change KellySizingConfig.fee_rate default from 0.0 to MARKET_COST_MODEL.round_trip_fraction()
   - Fix net_return in calculate_variance_estimate from 0.95 to (1 - ROUND_TRIP_FEE)

3. In position_sizing.py:
   - Change PositionSizingConfig.fee_rate default from 0.0 to 0.0205

4. In trade_execution.py:
   - Fix line 460: compute quantity first, then fee_cost = abs(quantity * self.fee_rate)

5. In paper_trading_engine.py:
   - Fix line 1738: same quantity-based fix

6. In pnl_tracking.py:
   - Fix line 252: use quantity-based fee calculation

7. In kelly_position_sizer.py:
   - Change DEFAULT_COST_FRACTION from 0.031 to MARKET_COST_MODEL.round_trip_fraction()

8. In fee_aware_filter.py:
   - Rename FEE_RATE to COMMISSION_RATE for clarity, add note that spread handles real cost

ROUTING: Route items 1-7 to Gilfoyle. Item 8 is documentation only.
================================================================================